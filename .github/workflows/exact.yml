name: exact-core

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python exact/generate_atlas.py
      - run: python exact/verify_core.py
      - run: git diff --exit-code -- data certificates
