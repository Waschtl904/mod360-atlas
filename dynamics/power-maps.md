# Potenzabbildungen

Familie: `f_k(a) = a^k mod 360`.

## Parameterstandard

Jede Berechnung nennt `k`, Definitionsbereich, betrachtete Iterationslänge und Normalisierung. Exponenten sollen zunächst modulo geeigneter Gruppenexponenten reduziert werden, soweit der Definitionsbereich dies erlaubt.
